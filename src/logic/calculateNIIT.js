// src/logic/calculateNIIT.ts
/**
 * MVP Stub — NIIT not implemented in RothMax 2025.
 * Always returns zero tax and zero thresholds.
 */
export function calculateNIIT( /* input: DomainInput */) {
    return {
        threshold: 0,
        magiOver: 0,
        nii: 0,
        taxableBase: 0,
        tax: 0,
    };
}
